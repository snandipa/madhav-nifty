

<table>
  <tr>
    <th>Name</th>
    <th>Department</th>
  </tr>
  <% for course in @courses %>
    <tr>
      <td><%= course.name %></td>
      <td><%= course.department %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Course", new_course_path %></p>
